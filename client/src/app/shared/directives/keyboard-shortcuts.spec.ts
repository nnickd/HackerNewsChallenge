import { KeyboardShortcuts } from './keyboard-shortcuts';

describe('KeyboardShortcuts', () => {
  it('should create an instance', () => {
    const directive = new KeyboardShortcuts();
    expect(directive).toBeTruthy();
  });
});
