import { KeyboardShortcutsDirective } from './keyboard-shortcuts.directive';

describe('KeyboardShortcuts', () => {
  it('should create an instance', () => {
    const directive = new KeyboardShortcutsDirective();
    expect(directive).toBeTruthy();
  });
});
